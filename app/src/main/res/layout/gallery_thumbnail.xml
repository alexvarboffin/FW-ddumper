<?xml version="1.0" encoding="utf-8"?>
<com.walhalla.common.ui.helper.SquareLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:background="@android:color/holo_red_dark">

    <!--android:scaleType="centerCrop" -png-->
    <ImageView
        android:id="@+id/thumbnail"
        android:layout_width="@dimen/vector_size"
        android:layout_height="@dimen/vector_size"
        android:layout_centerInParent="true"
        android:adjustViewBounds="true"
        android:contentDescription="@string/app_name"
        android:scaleType="centerCrop"
        tools:srcCompat="@drawable/ic_reboot" />

    <!--<ImageView-->
    <!--android:id="@+id/overflow_menu"-->
    <!--android:layout_width="@dimen/ic_album_overflow_width"-->
    <!--android:layout_height="@dimen/ic_album_overflow_height"-->
    <!--android:layout_alignParentEnd="true"-->
    <!--android:layout_alignParentRight="true"-->
    <!--android:layout_marginTop="@dimen/ic_album_overflow_margin_top"-->
    <!--android:contentDescription="@+id/overflow_menu"-->
    <!--android:scaleType="centerCrop"-->
    <!--android:visibility="gone"-->
    <!--android:src="@drawable/ic_dots" />-->
    <TextView
        android:padding="4dp"
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:background="#99000000"
        android:textAlignment="center"
        android:textAppearance="@style/Base.TextAppearance.AppCompat.Medium"
        android:textColor="@android:color/white"
        tools:text="@string/app_name"
        android:layout_alignParentLeft="true" />
</com.walhalla.common.ui.helper.SquareLayout>